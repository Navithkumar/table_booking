# ---------- Core FastAPI ----------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# ---------- Database (PostgreSQL, async) ----------
sqlalchemy>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.7   # only if you still use sync tools/migrations

# ---------- Migrations ----------
alembic>=1.13.1

# ---------- Redis ----------
redis>=5.0.1

# ---------- Background tasks / Messaging ----------
celery>=5.3.6
kombu>=5.3.5

# ---------- RabbitMQ client (optional direct usage) ----------
aio-pika>=9.4.0

# ---------- Environment & Settings ----------
python-dotenv>=1.0.1
pydantic-settings>=2.1.0

# ---------- Auth & Security ----------
python-jose[cryptography]>=3.3.0
passlib[argon2]

# ---------- CORS ----------
fastapi[all]  # includes CORS middleware & extras

# ---------- File & Image handling ----------
pillow>=10.2.0
python-multipart>=0.0.9

# ---------- HTTP client ----------
httpx>=0.27.0
requests>=2.31.0

# ---------- Utilities ----------
orjson>=3.9.12
